[package]
name = "cavestory_save_editor"
version = "1.0.0"
edition = "2021"
description = "profile.dat editor compatible with cavestory(+)"
authors = ["poly000"]
license-file = "LICENSE"

[dependencies]
inquire = "0.2.1"

[profile.release]
opt-level = "z"
strip = true
codegen-units = 1
panic = "abort"
